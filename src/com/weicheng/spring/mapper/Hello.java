package com.weicheng.spring.mapper;

public class Hello {

	/**
	 * 演示spring整合mybatis后mapper方式的dao层开发
	 * 
	 * 步骤：
	 * 	1、编写mapper.xml
	 * 	2、编写mapper.java接口，需要与mapper.xml同名且在同一个目录下
	 * 	3、在applicationContext.xml中注册mapper（单个注册或批量扫描都可以）
	 * 	4、编写测试类
	 * 
	 * 
	 * */
	
}
